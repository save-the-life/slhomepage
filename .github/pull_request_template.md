## What changed

<!-- One or two lines. What does a visitor to the site see differently? -->

## Checks

- [ ] `node scripts/verify.mjs` passes locally
- [ ] If any `data-i18n` string was added or reworded, the key exists in **all six** locales (ko, zh, ja, es, ru, vi)
- [ ] Copy changes were eyeballed in at least one non-English locale — Korean and Russian wrap differently and break headlines that look fine in English
- [ ] No internal or unpublished document was added to the repo (it is public; internal material belongs in `docs/`, which is gitignored)

## Notes

<!-- Anything a reviewer should know: claims that need verifying, numbers taken
     from a source document, decisions you made and would happily see reversed. -->
